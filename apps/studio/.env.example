# API Configuration
# All traffic goes through Gateway — auth, namespacing, host dispatch
KB_API_BASE_URL=http://localhost:4000/api/v1

# Gateway auth token (staticTokens in .kb/kb.config.json → gateway.staticTokens)
KB_GATEWAY_TOKEN=dev-studio-token

# Data Source Mode
# Options: 'mock' | 'http'
KB_DATA_SOURCE_MODE=http
