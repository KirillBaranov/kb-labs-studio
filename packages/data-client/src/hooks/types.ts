/**
 * @module @kb-labs/data-client/hooks/types
 * Types for hooks
 */

/**
 * Job event types
 */
export type JobEventType = 'job.queued' | 'job.started' | 'job.progress' | 'job.finished' | 'job.failed';

