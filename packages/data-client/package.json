{
  "name": "@kb-labs/data-client",
  "version": "0.1.0",
  "private": true,
  "description": "KB Labs Studio data client — API SDK with versioned contracts, mocks, and TanStack Query hooks",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "clean": "rimraf dist && find src -name '*.js' -type f ! -path '*/node_modules/*' -delete && find src -name '*.js.map' -type f ! -path '*/node_modules/*' -delete",
    "build": "pnpm clean && tsup",
    "type-check": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint . --max-warnings=0",
    "prebuild": "pnpm clean"
  },
  "dependencies": {
    "@tanstack/react-query": "^5.0.0",
    "@kb-labs/api-contracts": "link:../../../kb-labs-rest-api/packages/api-contracts",
    "zod": "^3.23.8",
    "ulid": "^2.3.0"
  },
  "devDependencies": {
    "@kb-labs/devkit": "github:KirillBaranov/kb-labs-devkit#main",
    "@types/node": "^24.3.3",
    "@types/react": "^18.3.8",
    "eslint": "^9",
    "react": "^18.3.0",
    "rimraf": "^6.0.1",
    "tsup": "^8.5.0",
    "typescript": "^5.6.3",
    "vitest": "^3.2.4"
  },
  "peerDependencies": {
    "react": "^18.3.1"
  }
}
